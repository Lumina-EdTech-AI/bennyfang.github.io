# Benny Fang Portfolio (Vue)

This project uses Vue 3 via a CDN and modular components, so it runs without a
local build step.

## Getting started

```bash
python -m http.server 4173
```

Then visit `http://localhost:4173`.
